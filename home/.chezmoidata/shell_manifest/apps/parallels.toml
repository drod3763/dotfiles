[package_catalog.packages."parallels"]
os = "mac"
type = "common"
target_class = "host"
host_only = true
# Installation note: your terminal needs Full Disk Access or the
# Parallels installer will fail during setup.
brew_cask_name = "parallels"
